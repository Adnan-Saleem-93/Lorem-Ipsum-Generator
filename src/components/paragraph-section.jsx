import React from "react";
import {lorem_ipsum} from "../utils/constants";

const ParagraphSection = ({value}) => {
  const renderParagraphs = () => {};

  return <>{renderParagraphs()}</>;
};

export default ParagraphSection;
